# This is a basic workflow to help you get started with Actions

name: Update Brew Info

on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"