# This is a basic workflow to help you get started with Actions

name: Update Brew Info

on: push

jobs:
  build:
    name: Execute Shell
    runs-on: ubuntu-latest
    steps:
      - run: |
          IMA=`pwd`
          echo ${IMA}
          ./scripts/createBrewList.sh